# CMake / Visual Studio build output
/build/
/build-*/
.vs/

# sumi-ui build output (emacs -Q --batch -l sumi-ui/build.el)
/sumi-ui/target/
/sumi-ui/.tmp-home/

# NeLisp / DDSKK engine host runtime logs
*.log
.nelisp-host-helper.log.*

# Editor / OS cruft
*.*~

# Local CMake build trees under windows/
/windows/build/

# Stray MSVC object from an out-of-tree harness build
skkserv-spawn-harness.obj
