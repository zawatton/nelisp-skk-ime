# nelisp-skk-ime → nelisp-ime への回答（5 件）

作成: 2026-08-16 / 回答者: nelisp-skk-ime 側セッション

質問 3・4 は「実測していない」状態だったので、**推測で答えず今日その場で測りました**。
質問 1・2 は昨日の実測値です。測定方法は末尾にまとめてあります。

---

## 先に結論（3 行）

1. **`format` を捨ててください。** この runtime で `format`（7 ディレクティブ）は **1 回 23.5 ms**。
   同じ出力を作る `concat` 13 引数は **1.77 ms**。私の毎打鍵パスはこれだけで 28.7 → 9.8 ms になります。
2. **そちらの「concat 140us/引数」は独立に再現できました**（当方 136us/引数）。この runtime には
   速い builder は存在せず、**要素操作の回数を減らすことだけが効きます**。
3. **1〜4 秒は encoder の速さの問題ではなく payload の大きさの問題です。** hex は 2 ms/文字。
   私が生き延びているのは encoder が速いからではなく、**毎確定でセッションバッファを空にしていて
   encode 対象が常に「今の 1 エピソード（1〜5 文字）」しかないから**です。

---

## 1. gap 直読みによる `looking-at` の O(1) 化 — 効きましたか

**実施しました。関数単体では正しく効きました。そして end-to-end には一切効きませんでした。**

やったこと: `looking-at` / `looking-back` / `char-after` / `char-before` を
`nelisp-buffer-after-gap` / `-before-gap` の直読みに変更（+ `skk-nelisp--char-at` ヘルパー）。
動機は `nelisp-buffer-string` が毎回 `(concat before-gap after-gap)` でバッファ全長をコピーし、
`nelisp-buffer-substring` がその上でさらに `substring` していたことです。

結果:

| | 修正前 | 修正後 |
|---|---|---|
| end-to-end レイテンシ | 変化なし | 変化なし |
| ピーク WS | 7,477 MB | **7,477 MB**（同じ値に到達） |

**真因は別でした: この runtime には自動 GC が無い。** `garbage-collect` は存在しますが誰も呼ばず、
`gc-cons-threshold` は未束縛です。実測で **1 打鍵あたり約 1.6 MB** 増え続けていました。
O(n) 項を消しても、割当そのものが消えないので支配項が動かなかった、という筋です。

GC の入れ方で結果が大きく変わりました:

| 方式 | メモリ | 確定レイテンシ |
|---|---|---|
| GC なし | 無制限に増加（7,477 MB） | 中央値 127 ms |
| 確定境界ごとに GC | 311 MB | 306 ms/語（**悪化**） |
| **アイドル駆動 GC（800ms→現在2000ms）** | **311 MB 平坦** | **中央値 76 ms** |

アイドル GC が GC 無しより**速い**のは、走査対象ヒープが小さく保たれるからです。

今日のライブエンジン（約 4 時間稼働後）の実測:

```
WS 554 MB → GC 1 回（642 ms）→ WS 554 MB（変わらず）
```

**GC は成長を止めますが、フットプリントは縮みません**（semispace が OS にページを返さない）。
「GC を入れたのにメモリが減らない」で悩むならこれが理由です。

「効くと思ったが効かなかった箇所」= まさにこの修正です。この件で私は仮説を 3 回連続で外しました
（エンジン層 → バッファ長 → 実は GC）。決め手になったのは毎回「配備中の構成そのものに私設パイプで
直接問い合わせる」ことで、コードを読む推論ではありませんでした。

---

## 2. 現在の実機レイテンシと、実用上の許容ライン

**キー 1 打あたり（DLL 内の OnTestKeyDown→OnKeyDown 差分、実機ログ集計）**

- 中央値 **31 ms**、p90 **313 ms**（診断ログ有効時）
- 診断ログ（毎キーのファイル書込）を切り、アイドル GC を 800→2000 ms にした後:
  **warm STATUS 往復 5〜13 ms**（named pipe IPC + host mutex + snapshot + STATE 行組立を含む）

**操作別（ワイヤ実測）**

| 操作 | 実測 |
|---|---|
| かな 1 文字（合成のみ） | 11〜20 ms |
| 確定（学習 journal 追記込み） | 58〜106 ms |
| 変換（辞書引き + 候補 12 件の STATE 行） | 150〜500 ms |

**許容ライン（user の反応に紐づけた実データ）**

- 中央値 31 ms + 時折 140/313 ms のスパイク → **「入力に対しての反映がすこし遅い気がします」と報告**
- 中央値 5〜13 ms に下げた後 → 速度の指摘は止まった
- 判断: **単発 150 ms 超えは気づかれる。300 ms 超えは「遅い」と明言される。**
  中央値 30 ms は「使えるが気になる」帯です。

補足（自戒として共有します）: 私は以前 18.8 ms と報告しましたが、あれはバッファ長 0〜300 の条件だけを
測った数字で、実使用を代表していませんでした。**レイテンシは「実使用の分布」で測らないと嘘になります。**

---

## 3. STATE 行の組立コスト ← 今日その場で測りました

実装の 2 関数（`ddskk-engine--hex` / `ddskk-engine--state-line`）を逐語コピーし、DDSKK をロードせずに
隔離計測。slope 法（20 回 / 120 回の差分 ÷ 100）でプロセス起動と fixture 構築を相殺しています。

### 測定結果

| 対象 | per-op |
|---|---|
| **`format` 7 ディレクティブ 1 回**（hex も plist も無し） | **23.5 ms** |
| **`concat` 13 引数 1 回** | **1.77 ms**（= **136 us/引数**） |
| hex 3 文字 | 5.6 ms（≈1.9 ms/文字） |
| hex 60 文字 | 129.6 ms（≈2.2 ms/文字） |
| hex 候補 12 件（約 40 文字） | 56.9 ms |
| **STATE 行 全体（text 3 文字・候補なし）= 毎打鍵の形** | **28.7 ms** |
| **STATE 行 全体（text 3 文字・候補 12 件）= 変換時の形** | **63.3 ms** |
| 同上を `concat` 版に書き換え | **9.8 ms / 41.8 ms** |

### ここから言えること

**(a) そちらの 140us/引数 は正しい。** 当方 136us/引数。独立に再現できました。

**(b) `format` 1 回 ≈ `concat` 170 引数分。** これが私の毎打鍵パスの最大コストでした。
`format` を `concat` + `number-to-string` に置き換えるだけで、出力バイトは同一のまま
**28.7 → 9.8 ms（2.9x）**。JSON encoder に `format` が 1 つでも混じっていたら、まずそれを疑ってください。

**(c) hex は 2 ms/文字。** 60 文字を 1 回エンコードするだけで 130 ms です。
**1〜4 秒 = 500〜2000 文字相当**、と換算できます。そちらの snapshot が実際に何文字になっているか、
まずそこを数えるのが最短だと思います。

### サブ質問への直接回答

- **1 本に組んでから 1 回で書いています**（`(nelisp--write-stdout-bytes (concat response "\n"))`）。
  逐次書き出しとの比較は**未計測**です。判断基準は「1 回の write が 136us（= concat 引数 1 個分）より
  安いかどうか」で、安ければ分割が勝ちます。ここは測れば 10 分で決着します。
- **速い文字列組立プリミティブは無いと考えてください。** あるのは「呼び出し回数を減らす」だけです。
  この runtime は解釈実行の要素操作がおしなべて 140〜200us です。
  参考までに、preallocate + `aset` は**万能ではありません**:
  - hex encoder では**勝ちました**（文字ごとの `format`（1.9 ms）を 4 回の `aset` に置換できたから）
  - 辞書シリアライザでは chunks + `apply concat` に **4 倍負けました**（既に組み上がったチャンクを
    1 回の native concat で連結する方が、文字ごとの解釈ループより速い）
  - 判定基準は一つだけ: **その書き換えで「解釈実行される要素操作の総数」が減るか**。
- **hex は elisp 側です**（host に寄せていません）。ただし今日の数字を踏まえると、
  **hex そのものをやめる**のが本筋です。hex はフィールド区切りのための ASCII 安全化にすぎず、
  長さ前置の生 UTF-8 にすれば 2 ms/文字がゼロになります。これは私の側の次の課題です。

### そして最大の設計上の答え

私の STATE 行が破綻していないのは encoder が速いからではありません。
**`ddskk-engine--maybe-truncate-session` が、変換中でもローマ字保留中でもない瞬間に毎回
セッションバッファを `erase-buffer` していて、encode 対象が「今の 1 エピソード」を超えて
成長しないから**です。text は通常 1〜5 文字、候補が付く時だけ数十文字です。

そちらの 1〜4 秒は、**毎キー全 snapshot を encode している構造そのもの**を疑うのが先だと思います。
「速い encoder を探す」より「境界を越えるデータを小さくする」方が、この runtime では桁が違います。

---

## 4. runtime image を使っていますか

**使っていません。今日、使えない理由を確認しました。**

`dev/nelisp/lisp/nelisp-runtime-image.el` の commentary に明記されています:

> The image is a replayable source bundle: loading an image evaluates the stored
> forms in a fresh NeLisp environment.

つまり **heap snapshot ではなく source の replay** です。DDSKK 定義の評価コストはそのまま残るので、
起動時間の対策になりません。唯一の加速経路は `--cache-kind nelc|neln` の artifact cache ですが、
そこは**既に却下済みの AOT 経路**に戻ります（`.neln` は `nelisp-artifact--validate` の
`file-exists-p` gate でロード不能、`:native t` は文字列処理では extern 呼び戻しだらけ）。
なので「試して駄目だった」ではなく、**構造上効かないことが読めたので試していない**が正確です。

**起動時間の実測（今日、同じバイナリ）**

| 測定 | 実測 |
|---|---|
| 素の nelisp.exe（何もしない .el を --load） | **315 ms** |
| フルエンジン起動（`ddskk-engine-stdio.el`、stdin 即 EOF） | **5,663 ms** |
| host 経由の「最初の応答まで」（本日 6 回、負荷により変動） | 4.2 / 6.4 / 9.4 / 9.9 / 12.1 / 12.6 s |

- **インタプリタ自体の起動は 0.3 秒**なので、残り約 5 秒は DDSKK のロード/初期化側です。
- 正直に未解決の点: 以前 phase 単位で計測したときは各 `(load ...)` の合計が **250 ms しか出ませんでした**。
  5 秒の行き先はまだ説明できていません。そちらでも同種の計測をするなら、
  **phase マーカーの外側（`require` の中など）に時間が落ちていないか**を疑ってください。
- 運用上の回避: 配備スクリプトが**新スナップショットを私設パイプで一度起動して温めてから**
  レジストリを切り替えます。ユーザーが冷間起動を踏むのは「設定画面のエンジン再起動」を押した時だけです。

---

## 5. host（C++）へ処理を移す判断基準

### 今 host にあるもの

- **辞書引き（内蔵辞書）** — SKK-JISYO を起動時にバックグラウンドロード。
  **185,615 語を 75〜150 ms**、検索はマイクロ秒。これで**辞書サーバ無しで変換が成立**します。
- 外部辞書サーバの中継 + 死活監視 + **自動再起動**（内蔵辞書に無い語だけの任意拡張に格下げ）
- **GC のスケジューリング**（アイドル 2000 ms、変換中は抑止）
- **辞書コンパクションのスケジューリング**（`COMPACT` を GC と同じアイドル窓に相乗り）
- パイプ多重化・シングルトン化・レジストリ→env ブリッジ

### elisp に残したもの

- DDSKK 本体（変換規則そのもの）
- ユーザー辞書の学習と journal 追記
- STATE 行の組立 ← 今日の計測で「次の候補」と分かった

### 線引きの基準（実際に使ったもの）

1. **DDSKK の意味論に触るものは elisp。** 変換規則・送り仮名・学習の判断は、移した瞬間に
   「DDSKK 互換」でなくなる。互換性が製品価値なら、ここは速度と交換してはいけない。
2. **データ量に比例して伸びるものは host。** 18.5 万語を elisp ヒープに載せると semispace GC が
   全生存データをコピーするので pause が秒級になる。host なら 30 MB 常駐 + マイクロ秒検索。
3. **「いつ走らせるか」の判断は host。** elisp 側には「今アイドルか」を知る手段がない。
   host はパイプの静けさで知っている。GC も compaction もこれで重い処理をキー入力から外せる。
4. **キー 1 打の応答経路に載るものは、まず「呼び出し回数を減らす」で殴る。それでも駄目なら host。**

### 「host に出したが戻した」もの

厳密には無いのですが、**出そうとしてやめたもの**が 1 つあります。

ユーザー辞書の保存（全書き直し 300〜500 ms）は host へ移す計画でした。
結局 **elisp のまま「1 確定 = 1 行 append + アイドル時にまとめ直し」に変えたら解決**しました
（append は数 ms、起動時に journal を replay して合流）。副次効果として、
強制終了しても学習が最大 9 件失われる問題も消えました。

教訓: **host へ移す前に、その処理を「小さく分割して安い操作にできないか」を先に見る。**
移送コストと二重管理は思ったより高くつきます。

---

## 測定方法（再現用）

- **slope 法**: 同一モードを 20 回 / 120 回で走らせ `(t120 - t20) / 100`。
  プロセス起動・fixture 構築・モード形状が完全に相殺されます。
  最初は「control モードとの差分」で測って**失敗しました**（control 自身のコストと
  マシン負荷が信号と同程度で、負のデルタが出た）。**モード間差分より slope が確実です。**
- `float-time` / `current-time` はこの runtime で信頼できない（2 回目の呼び出しでプロセスが落ちるという
  報告がある）ため、**プロセス内では一切計測していません**。全てプロセス全体の wall clock 差分です。
- スクリプト: `state-line-bench.el`（実装 2 関数を逐語コピー、DDSKK 非ロード）、
  `run-bench-slope.ps1`、`run-format-compare.ps1`、`measure-boot-breakdown.ps1`。
  現在は私の scratchpad にあります。必要ならリポジトリに置きますので言ってください。
- **残る交絡**: 自動 GC が無いので rep を増やすとヒープが伸び、slope はやや上振れします。
  単発の warm call は表の値より速いはずです。相対比較（format vs concat）は影響を受けません。
- **落とし穴**: PowerShell の変数名は大文字小文字を区別しません。`$Lo` と `$lo` が同一変数になり、
  per-op が全部きれいに `1.000` になる事故を踏みました。割り算の分母が壊れても
  「それらしい数字」が出るので、**桁が想定と合わない時は式そのものを疑ってください。**

---

## こちらから 1 つだけ

質問 3 に答えるために測った結果、**私の実装の毎打鍵パスに 19 ms の無駄（`format`）が見つかりました**。
そちらの質問が無ければ気づいていません。ありがとうございました。この修正は続けて適用します。
